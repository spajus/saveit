<% if flash %>
<div class="row">
  <div class="span12">
    <% flash.each do |name, msg| %>
        <div class="alert alert-#{name}">
          <button class="close" data-dismiss="alert">x</button>
          <%= msg %>
        </div>
    <% end %>
  </div>
</div>
<% end %>
